; Deauth Attack Challenge - Client
;
; Connects and stays connected. Only reconnects when kicked.
; Participants must deauth this client to capture handshake.

[env:d1_mini]
platform = espressif8266
board = d1_mini
framework = arduino
